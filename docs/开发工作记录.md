# 开发工作记录

本文件记录仓库中可核验的开发阶段。提交时间使用 Git 实际提交时间，不回填或伪造历史日期。

## 提交阶段

| 阶段 | 内容 | 主要路径 | 验证 |
|---|---|---|---|
| 1 | 项目基础配置和运行入口 | `.gitignore`、`README.md`、`config.py`、`run.py` | 项目结构检查 |
| 2 | Flask 扩展、数据模型和时间工具 | `app/extensions.py`、`app/models.py`、`app/time_utils.py` | Python 编译 |
| 3 | 管理员登录、推荐和评论分析基础 | `app/auth.py`、`app/recommend.py`、`app/analysis.py` | Python 编译 |
| 4 | 演唱会查询、概览接口和业务服务 | `app/routes.py`、`app/services.py` | Flask 测试 |
| 5 | 后台管理接口 | `app/admin_routes.py` | 后台接口测试 |
| 6 | 分析聚合和 CSV 校验 | `app/analytics.py`、`app/validation.py` | 六类分析接口测试 |
| 7 | 页面模板和本地静态资源 | `app/templates/`、`app/static/` | 模板和脚本检查 |
| 8 | 数据采集、清洗、分析和导入脚本 | `scripts/` | Python 编译 |
| 9 | 原始公开数据与来源核验快照 | `data/raw/` | CSV 数量和字段检查 |
| 10 | 自动加载本地公开快照 | `app/__init__.py`、`app/services.py` | 新数据库启动测试 |
| 11 | 功能测试和构建工具 | `tests/`、`tools/` | `pytest -q`、DOCX 构建 |
| 12 | 技术实施文档和设计记录 | `docs/`、`design-system/` | Markdown 和 DOCX 完整性检查 |
| 13 | Trellis 项目规范和协作记录 | `.trellis/`、`.agents/`、`.gitattributes` | 配置文件检查 |

## 当前数据记录

- 演唱会原始记录：78 条。
- 艺人：57 位。
- 城市：12 个。
- 评论：35 条。
- 本次公开来源核验：9 个，均返回 HTTP 200。
- 数据来源明细：`data/raw/public_sources.csv`。
- 新增数据批次：`data/raw/concerts_public.csv`。

## 实际提交记录

以下提交均产生于 2026-08-19，已推送到公开仓库：

- `5eb4c8e` 项目配置和运行入口
- `0887494` 数据库模型和时间工具
- `8e81614` 管理员认证、评论分析和规则推荐
- `d3ca3da` 业务服务和公开概览接口
- `9fbbc0f` 后台数据治理接口
- `7080dfe` 分析聚合和 CSV 校验
- `6fb90d9` 本地公开快照自动加载
- `887a7d2` 页面模板
- `415789c` 图表、交互脚本和深色样式
- `acf31d3` 数据采集和处理脚本
- `955c48e` 多艺人公开演唱会数据
- `45c9c22` 自动化测试和文档构建工具
- `c9b5e3a` 技术文档和设计记录
- `46cb5ca` 项目规范和协作记录

公开仓库：<https://github.com/ldt471146/concert-data-visualization>

## 验证记录

最近一次验证结果（2026-09-01）：

- `pytest -q`：**21 passed**（覆盖健康检查、概览、城市筛选、十四项分析接口、批量分析接口、场次详情与评论分页、关键字搜索、艺人趋势、管理员登录鉴权、后台统计/分页/导出、数据导入、快照规模断言等）。
- `node --check app/static/js/app.js`：通过。
- Python 编译检查：通过。
- `docs/技术实施文档.docx`：重建并完成 ZIP 完整性检查。
- `/api/health`：数据库连接正常，场次 18781（演唱会 620）、评论 117242，合计 136023 条（超 10 万）。

## 最近改动记录

- `a99e185`（2026-09-01）性能修复：`run.py` 改 `debug=False` 单进程运行、SQLAlchemy 连接池放宽（20/40/60s）、新增 `/api/analytics/all` 批量接口（共享一次查询上下文）。浏览器打开从「14 并发请求冷启动 30s+ 超时打不开」修复为「2 请求冷启动 3.7 秒、缓存命中 16 毫秒」。
- `a78b0ad`（2026-09-01）新增五组分析面板：评论情感分布（-1~1 五档）、场次星期分布、售票状态占比、评论平台构成（按 source_url 域名识别，全量 SQL 聚合不受 3000 采样上限影响）、热门场馆 Top；分析中心扩为十四项。
- `977a293`（2026-08-31）交互增强：场次详情抽屉（票价明细 + 评论翻页 + 原页链接）、关键字搜索条、城市柱状图点击联动筛选；新增 4 个接口 `/api/concerts/<id>`、`/api/comments/page`、`/api/search`、`/api/analytics/artist-trend`。

后续新增功能应在完成后追加真实日期、提交编号、变更路径和验证结果，不使用虚构的历史时间。 
